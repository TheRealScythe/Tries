{
  "scripts": {
    "start": "tsc && node dist/api.js",
    "build": "tsc",
    "test": "node_modules/.bin/mocha --timeout 300000 -r ts-node/register src/**/*.test.ts",
    "coverage": "nyc -r lcov -e .ts -x \"*.test.ts\" npm run test",
    "bundle": "gulp bundle --gulpfile node_modules/paperback-extensions-common/src/repo-gulpfile.js"
  },
  "dependencies": {
    "chai": "^4.3.3",
    "chai-as-promised": "^7.1.1",
    "cheerio": "^1.0.0-rc.5",
    "paperback-extensions-common": "^3.0.2",
    "ts-mocha": "^8.0.0",
    "tsify": "^5.0.2",
    "typescript": "^4.2.3"
  },
  "devDependencies": {
    "@types/cheerio": "^0.22.26",
    "@types/mocha": "^8.2.1",
    "mocha": "^8.3.2",
    "nyc": "^15.1.0"
  }
}
